// Sky backdrop: just the page sky gradient for now.
// Cloud assets will go here once we have proper AI-generated PNGs.
export function SkyBackdrop() {
  return <div className="absolute inset-0 overflow-hidden sky-gradient" />;
}
